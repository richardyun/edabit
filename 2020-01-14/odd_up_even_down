// INSTRUCTIONS: Create a function that goes through the array, incrementing (+1) for each odd number and decrementing (-1) for each even number.

function transform(arr) {
	for (i=0; i<arr.length; i++) {
    if (arr[i] % 2 === 0) {
      arr[i] -= 1;
    } else arr[i] += 1;
  }
  return arr;
}