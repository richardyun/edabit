// INSTRUCTIONS: Write a function that returns true if an object is empty, and false otherwise.

function isEmpty(obj) {
	if (Object.keys(obj).length === 0) {
    return true;
  } else return false;
}