// INSTRUCTIONS: A set is a collection of unique items. A set can be formed from an array from removing all duplicate items.

// [1, 3, 3, 5, 5, 5]
// original array

// [1, 3, 5]
// original array transformed into a set

// Create a function that transforms an array into a set.

function set(arr) {
	const newArr = [...new Set(arr)];
  return newArr;
}