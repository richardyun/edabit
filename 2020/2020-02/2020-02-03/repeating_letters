// INSTRUCTIONS: Create a function that takes a string and returns a string in which each character is repeated once.

function doubleChar(str) {
  const arr = str.split("");
  const newArr = arr.map(x => x + x);
  return newArr.join("");
}