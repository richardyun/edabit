// INSTRUCTIONS: Create a function that takes two numbers as arguments (num, length) and returns an array of multiples of num up to length.

function arrayOfMultiples (num, length) {
  const arr = [];
	for (let i = 0; i < length; i++) {
    arr.push(num + (num * i));
  }
  return arr;
}