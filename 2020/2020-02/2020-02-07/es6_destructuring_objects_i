// INSTRUCTIONS: In javascript you can do basic object assignment like this:

// const obj =  { one : 1, two : 2 }

// let one = obj.one
// let two = obj.two

// However, with es6 you can assign the variables in a much more succint way.

// Challenge

// -Use es6 object destructuring to assign variables one and two to obj.one and obj.two respectively.
// -Although you can use let, var, or const for assignment, DO NOT use these in this challenge.
// -Ignore the .toString() function (used for validation)

let str = `( {one, two} = { one : 1, two : 2}).toString()`