// INSTRUCTIONS: Given an object of how many more pages each ink color can print, output the maximum number of pages the printer can print before any of the colors run out.

function inkLevels(inks) {
	const arr = Object.values(inks);
  return Math.min(...arr);
}