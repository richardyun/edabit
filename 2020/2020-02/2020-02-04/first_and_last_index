// INSTRUCTIONS: Given a word, write a function that returns the first index and the last index of a character.

function charIndex(word, char) {
	if (word.indexOf(char) >= 0) {
    return [word.indexOf(char), word.lastIndexOf(char)];
  } else return undefined;
}