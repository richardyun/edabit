// INSTRUCTIONS: Given two strings, create a function that returns the total number of unique characters from the combined string.

function countUnique(s1, s2) {
	const newStr = s1 + s2;
  return String.prototype.concat(...new Set(newStr)).length;
}