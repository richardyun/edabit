// INSTRUCTIONS: I'm trying to watch some lectures to study for my next exam but I keep getting distracted by meme compilations, vine compilations, anime, and more on my favorite video platform.

// Your job is to help me by creating a function that takes in a string and checks to see if it contains the following words or phrases:

// -"anime"
// -"meme"
// -"vine"
// -"roasts"
// -"Danny DeVito"

// If it does, return "NO!". Otherwise, return "Safe watching!".

function preventDistractions(str) {
	const regex = /(anime)|(meme)|(vine)|(roasts)|(Danny DeVito)/;
  if (regex.test(str)) {
    return "NO!";
  } else return "Safe watching!";
}