// INSTRUCTIONS: Create a function that takes an array of strings and returns the words that are exactly four letters.

function isFourLetters(arr) {
	const fourLetters = arr.filter(word => word.length === 4);
  return fourLetters;
}