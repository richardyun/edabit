// INSTRUCTIONS: Create a function that takes in an initial word and filters out an array to contain words that start with the same letters as the initial word.

function dictionary(initial, words) {
	return words.filter(x => x.startsWith(initial));
}