// INSTRUCTIONS: Create a function that takes an array of students and returns an array of student names.

function getStudentNames(students) {
  const nameArray = [];
	for (i=0; i<students.length; i++) {
    nameArray.push(students[i].name);
  }
  return nameArray;
}