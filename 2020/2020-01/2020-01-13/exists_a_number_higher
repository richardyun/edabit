// INSTRUCTIONS: Write a function that returns true if there exists at least one number that is larger than or equal to n.

function existsHigher(arr, n) {
	if (Math.max(...arr) >= n) {
    return true;
  } else return false;
}