// INSTRUCTIONS: Write a function that returns true if all characters in a string are identical and false otherwise.

function isIdentical(s) {
	const arr = s.split('');
  const uniques = [...new Set(arr)];
  if (uniques.length === 1) {
    return true;
  } else return false;
}