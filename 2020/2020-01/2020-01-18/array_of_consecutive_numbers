// INSTRUCTIONS: Implement a function that returns an array containing all the consecutive numbers in ascendant order from the given value low up to the given value high (bounds included).

function getSequence(low, high) {
	const arr = [];
	for (let i=low; i<=high; i++) {
		arr.push(i);
	}
	return arr;
}