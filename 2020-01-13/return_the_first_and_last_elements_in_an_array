// INSTRUCTIONS: Create a function that takes an array of numbers and return the first and last elements as a new array.

function firstLast(arr) {
	const newArray = [];
  newArray.push(arr[0]);
  newArray.push(arr[arr.length - 1]);
  return newArray;
}