// INSTRUCTIONS: Create a function to multiply all of the values in an array by the amount of values in the given array.

function MultiplyByLength(arr) {
	const newArray = arr.map(el => el * arr.length);
  return newArray;
}