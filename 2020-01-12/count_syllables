// INSTRUCTIONS: Create a function that counts the number of syllables a word has. Each syllable is separated with a dash -.

function numberSyllables(word) {
	const syllableArray = word.split("-");
  return syllableArray.length;
}